# 🎁 WB Giveaway Bot

Telegram бот для розыгрышей сертификатов Wildberries с системой автоматического догрева.

## 📋 Функционал

- ✨ **Стартовое сообщение** — привлекательное приветствие с информацией о призах
- 🎰 **Кнопка розыгрыша** — открывает WebApp с анкетой
- 🔥 **Система догрева** — автоматическая рассылка напоминаний:
  - Через 1 час после регистрации
  - Через 24 часа
  - Через 72 часа
- 📊 **Статистика** — команда /stats для админа
- 🔗 **Отслеживание источников** — параметр в ссылке для трекинга рекламы

## 🚀 Установка

### 1. Клонировать/скачать проект

```bash
cd botwb
```

### 2. Установить зависимости

```bash
npm install
```

### 3. Настроить окружение

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=ваш_токен_бота
WEBAPP_URL=https://ваш-url-webapp.com
```

### 4. Запустить бота

```bash
npm start
```

Для разработки (с автоперезагрузкой):

```bash
npm run dev
```

## 📱 Создание бота в Telegram

1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

## 🔗 Рекламные ссылки

Для отслеживания источников трафика используйте deep links:

```
https://t.me/ваш_бот?start=канал1
https://t.me/ваш_бот?start=канал2
https://t.me/ваш_бот?start=blogger_name
```

Источник сохраняется в базе данных для аналитики.

## ⚙️ Настройка догрева

В файле `.env` можно изменить задержки догрева:

```env
WARMUP_DELAY_1=1    # Первое напоминание через 1 час
WARMUP_DELAY_2=24   # Второе через 24 часа
WARMUP_DELAY_3=72   # Третье через 72 часа
```

## 📝 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню с информацией о розыгрыше |
| `/help` | Справка |
| `/status` | Статус участия |
| `/stats` | Статистика (только админ) |

## 🗃 Структура проекта

```
botwb/
├── src/
│   ├── bot.js        # Основной файл бота
│   ├── config.js     # Конфигурация и тексты
│   ├── database.js   # Работа с SQLite
│   └── warmup.js     # Система догрева
├── data/
│   └── users.db      # База данных (создаётся автоматически)
├── .env              # Настройки (создать из .env.example)
├── package.json
└── README.md
```

## 📊 База данных

SQLite хранит:
- Telegram ID пользователя
- Username, имя, фамилия
- Дата регистрации
- Статус участия
- Стадия догрева
- Источник (рекламный канал)
- Флаг блокировки бота

## 🔧 Доработки

### Изменение текстов

Все тексты находятся в `src/config.js` в объекте `messages`.

### Добавление Admin ID

В файле `src/bot.js` замените:

```javascript
const adminIds = [123456789]; // Ваш Telegram ID
```

Узнать свой ID можно через [@userinfobot](https://t.me/userinfobot)

## 📄 Лицензия

MIT
